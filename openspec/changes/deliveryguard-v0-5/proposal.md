# Optional Jev review adapter

Implement the public TypeSafe API as an optional adapter with summary disclosure, bounded requests, strict Noul parsing, evidence persistence and user-managed credentials. Keep core checks offline.
