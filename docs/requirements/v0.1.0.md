# DeliveryGuard v0.1.0 developer preview

The first public preview provides a configuration-driven CLI, version gates, acceptance evidence validation, Repair Cases, an OpenSpec adapter, Codex skills, and a fully synthetic example. It does not perform deployments, send messages, mutate business data, or host an agent runtime.
